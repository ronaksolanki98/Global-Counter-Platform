{
  "name": "serverless-counter-frontend",
  "version": "0.1.0",
  "description": "Static frontend for the serverless counter",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "eslint assets",
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "eslint": "^8.61.0"
  }
}
